server:
  port: 3435
spring:
  datasource:
    url: "jdbc:h2:~/.cache/todo.h2"
    driverClassName: "org.h2.Driver"
    username: "sa"
    password: "password"
    h2:
      console:
        enabled: true
    jpa:
      hibernate:
        ddl-auto: "update"
